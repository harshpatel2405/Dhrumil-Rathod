#include <stdio.h>

int main()
{
    // int age;

    // printf("Enter your age : ");
    // scanf("%d", &age);

    // printf("Age is %d", age);

    float a, b;
    int c;
    printf("Enter Three numbers : ");
    scanf("%f%f%d", &a, &b, &c);

    // scanf("%f", &a);
    // scanf("%f", &b);

    printf("A : %f\tB : %f\tC : %d", a, b, c);

    return 0;
}
