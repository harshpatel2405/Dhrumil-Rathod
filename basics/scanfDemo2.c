#include <stdio.h>

int main()
{
    // char a, b, c, d, e;

    // printf("Enter your name : ");
    // scanf(" %c %c %c %c %c", &a, &b, &c, &d, &e);

    // printf("Name is %c%c%c%c%c", a, b, c, d, e);

    // int age;
    // char grade;

    // printf("Enter age and grade : ");
    // scanf("%d", &age);
    // scanf(" %c", &grade);

    // printf("Age : %d\tGrade : %c", age, grade);

    int a = 90;
    int b = 78;

    int c = a + b;
    printf("Adition of %d and %d is %d", a, b, c);
    return 0;
}